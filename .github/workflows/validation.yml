name: Validation

on:
  pull_request:
  push:
    branches:
      - main
      - refresh

jobs:
  vanilla:
    name: Vanilla
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vanilla
    steps:
      - uses: ./.github/actions/app_validation.yml
