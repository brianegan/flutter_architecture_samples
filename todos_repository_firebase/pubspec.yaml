name: todos_repository_firebase
description: An implementation of the todos_repository_core with Firebase's Cloud Firestore as the backing store

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  todos_repository_core:
    path: ../todos_repository_core
  firebase_auth: 0.5.19
  cloud_firestore: 0.7.4

dev_dependencies:
  test: ^1.3.0
  mockito: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
